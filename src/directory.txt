marker_pdf/                                  # Root directory for Marker-based document processing
│
├── data/                                    # All input and output data used in the pipeline
│   │
│   ├── completed/                           # Processed - Raw standards PDFs
│   │
│   ├── input_pdfs/                          # Raw standards PDFs used as source documents
│   │
│   └── output/                              # Outputs generated after running Marker and scripts
│       │
│       ├── marker_json/                     # Raw JSON output directly generated by Marker
│       │
│       ├── output_json/                     # Clean JSON collected from marker_json folders
│       │
│       ├── output_schema/                   # Clause-hierarchical JSON schema built from output_json
│       │
│       ├── scope/                           # Contains scope of the processed PDFs for standards recommendation
│       │
│       ├── output_json_chunk/               # Contains chunks created clause wise from json schema
│       │
│       └── output_images/                   # Contains all images that are referenced in the json schema
│
├── src/                                     # All executable scripts for the Marker → RAG pipeline
│   │
│   ├── collect_json.py                      # Copies per-PDF Marker JSON into output_json
│   │
│   ├── json_to_schema_v4.py                 # Converts output_json into hierarchical clause schema
│   │
|   ├── directory.txt                        # this document itself
|   │
|   ├── path.py                              # Contains all the paths of the directory for Inputs and Outputs
|   │
|   ├── schema_to_chunks.py                  # converts all the json schema into chunks for knowledge graph creation
|   │
|   ├── extract_scope.py                     # code to extract all scope from the json of standard pdfs
|   │
|   └── marker_ini.py                        # initializing marker command line interface for processing documents